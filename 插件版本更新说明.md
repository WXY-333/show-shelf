# 展示架 1.7.0 更新说明

- 新增分类模板与自定义字段，内置动漫影视、美食、旅行、书单、运动、好物等常用模板，也支持自由创建模板。
- 新增 Bangumi 条目一键解析、前后台卡片/列表视图、前台三档卡片尺寸和新增内容默认位置设置。
- 评论区支持 Halo 官方评论组件、评论组件 Next 与 Twikoo，并支持页面底部评论和作品详情独立评论。
- Halo 官方评论组件与评论组件 Next 评论数据互通；新增匿名邮箱评论适配，开启后访客填写昵称即可提交。
- 新增可选访客统计、页面快速置顶/置底按钮，并统一跟随后台主题色。
- 优化移动端详情弹窗、评论回复、表情面板层级与裁切，以及 Comment Next 表情分类横向滑动性能。
- 优化评论组件切换与缓存兼容，后台保存切换后，已打开的展示页面能够及时同步新样式。

# 展示架 1.7.1 更新说明

新增后台展示内容搜索框（标题/简介/感受/状态/标签实时过滤）
新增 TMDB 链接解析（movie/tv 链接一键填入），API Key 在后台设置持久化
/movie页面 改用 Halo 原生模板渲染，使Live2D 等插件资源可以正常注入
修复“新增展示内容默认位置”保存后不回显

# 展示架 1.7.2 更新说明

修复内容：
- TMDB API Key 安全隔离
  - 新增后台专用接口 /admin/settings，后台控制台改为从该接口读取设置。
  - 原 /settings 接口继续保留，但返回数据会清空 tmdbApiKey，避免公开设置响应泄露密钥。
  - TMDB Key 仍通过 /admin/settings 保存和读取。
- 观看状态默认值修复
  - 新增展示卡片时，观看状态默认为空，不再自动填入“已看完”。
  - 编辑已有空状态卡片时，输入框保持空白。
  - 保存空状态后再次编辑，不会再被自动填回“已看完”。
  - 数据模型默认状态也已从“已看完”改为空字符串。